{
  "name": "apocrypha",
  "version": "0.0.1",
  "description": "an engine for building websites with markdoc",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "files": [
    "src/catalog.d.ts"
  ],
  "dependencies": {
    "@markdoc/markdoc": "^0.2.2",
    "chokidar": "^3.5.3",
    "fast-glob": "^3.2.12",
    "js-yaml": "^4.1.0",
    "vite": "4.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^18.15.3",
    "@types/react": "^18.0.28",
    "command-line-args": "^5.2.1",
    "esbuild": "^0.17.12",
    "jest": "^29.5.0",
    "tosource": "^2.0.0-alpha.3",
    "ts-jest": "^29.0.5",
    "typescript": "^5.0.2"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "bracketSpacing": false,
    "arrowParens": "always"
  },
  "scripts": {
    "build": "pnpm run build:library && pnpm run build:types",
    "build:library": "node build.js",
    "build:types": "tsc --emitDeclarationOnly --outDir dist",
    "dev": "node build.js --watch",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "test": "pnpx jest",
    "zen": "pnpx jest --watch"
  }
}
